to run on a local machine, make sure pip is installed, and create a virtual environment using venv

then run command pip install -r requirements.txt

once dependencies are installed run python3 manage.py runserver

app should be displayed on 127.0.0.1:8000/imageProcessor

test
